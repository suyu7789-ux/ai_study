# Python-generated files
# Python字节码缓存目录（≈ Java 的 target/classes)
__pycache__/ 
# 忽略 .pyo 和 .pyc 文件(≈ .class),
# [oc] 是通配写法,等于同时写 *.pyo *.pyc
*.py[oc]
# build/ dist/ wheels/ *.egg-info
# 打包发布时才生成的产物(≈ Java 打出来的 jar/build 目录)
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.DS_Store
