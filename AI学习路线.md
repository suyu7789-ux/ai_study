# AI 学习路线（Java 后端 → AI 应用 / Agent 工程师）

> **本文件用途**：这是我的 AI 学习"总纲 + 进度锚点"。
> 每次新开窗口时，让 Claude 先读这个文件，然后告诉它"我现在要做 Week X / 阶段 X"，即可无缝继续。
> Claude 完成任务后，请在对应任务的 `[ ]` 里打勾 `[x]`，并在文末"进度日志"追加一行。

---

## 我的背景 & 目标

- **身份**：应届毕业生，现从事 Java 后端开发（Spring Boot / MyBatis-Plus / Spring Cloud 技术栈）。
- **转型动机**：想学习 AI 技术，增强职业竞争力。
- **已有基础**：扎实的 Java 后端工程能力；用过 Codex / Claude Code 等 AI 编码工具。
- **目标方向**：**AI 应用工程师 / AI Agent 开发工程师**（复用后端工程优势，而非死磕算法岗）。
- **核心策略**：Java 后端 + AI 应用能力 = 稀缺组合。做加法，不推倒重来。
- **学习原则**：
  1. 每周必须有**产出物**，全部提交到一个 GitHub 仓库（简历雏形）。
  2. **以战代练**，动手优先，拒绝"只收藏不实践"。
  3. 善用 AI 工具（Codex/Claude）边做边学。
  4. 底层原理"够用即可"，不追求从零推导数学。

---

## 五阶段总览

| 阶段 | 主题               | 周期     | 核心目标                                       |
| ---- | ------------------ | -------- | ---------------------------------------------- |
| 一   | 打地基             | ~4 周    | Python + 调大模型 + Prompt + Function Calling  |
| 二   | AI 应用开发（RAG） | ~6-8 周  | 独立做出"知识库问答"应用                       |
| 三   | AI Agent 开发      | ~6-8 周  | 理解并能开发智能体（核心竞争力）               |
| 四   | 够用的底层原理     | 全程穿插 | 理解 Transformer / 幻觉 / 微调等，够对话够排查 |
| 五   | 工程化与求职       | 持续     | 部署 / LLMOps + 完整项目 + 找工作              |

**预计约 6 个月达到求职水平**（有工程基础可能更快）。

---

## 阶段一：打地基（约 4 周）

**总目标**：从"会用 AI 工具" → 变成"能用 Python 调大模型、写出可控的 AI 调用"。
**阶段产出**：一个包含 4 个小项目的 GitHub 仓库。

### Week 1：Python 快速上手（重点学与 Java 的差异）

- [X] 环境搭建：`uv` 或 `conda` + 虚拟环境（≈ Maven 管依赖）
- [X] 基础语法：`list / dict / set`（dict ≈ HashMap）
- [X] 列表推导式、`f-string`
- [X] 函数、`*args/**kwargs`、装饰器（≈ 注解 + AOP）
- [X] 类型注解 `def f(x: str) -> int`
- [X] `requests` 发 HTTP 请求（≈ RestTemplate/OkHttp）
- [X] **产出**：用 `requests` 调用任意公开 REST API 并打印结果

### Week 2：第一次调用大模型 API

- [X] 申请 API Key（Claude / 通义 / 智谱 / DeepSeek 任选）—— 选 DeepSeek（国内易注册、便宜、OpenAI 兼容）
- [X] 安装 SDK（用 `uv add openai`，DeepSeek 兼容 OpenAI SDK）
- [X] 理解请求结构：`system` / `messages`（多轮）/ `model` / `max_tokens`
- [X] 理解 `temperature`（0=稳定，1=随机；业务通常调低）—— 做了对比实验，附带认识到"真实 API 上 temp=0 也不保证完全可复现"
- [X] 流式输出 `stream=True`
- [X] 理解 token / 上下文窗口（计费 & 记忆容量单位）
- [X] **产出**：命令行多轮对话小程序（记住上下文 + 流式输出）

### Week 3：Prompt Engineering（性价比最高的一周）

- [X] 角色设定（System Prompt）
- [X] Few-shot（给示例提升准确率）
- [X] 思维链 CoT（"一步步思考"）
- [X] ⭐ 结构化输出（要求返回 JSON，后端最关键）
- [ ] 防幻觉技巧（"不确定就说不知道"、"只根据给定资料回答"）
- [ ] **产出**：简历信息提取器（输入简历文本 → 返回固定结构 JSON → Python 解析）

### Week 4：Function Calling / Tool Use（Agent 地基）

- [ ] 理解 Function Calling 概念（模型自己决定何时调用你的函数）
- [ ] 定义 tool 的 JSON schema（像写 API 文档）
- [ ] 走完整回合：模型请求调用 → 你执行 → 结果喂回 → 模型最终答复
- [ ] **产出**：天气助手（问"北京穿什么"→ 模型自动调 `get_weather()` → 给穿衣建议）
- [ ] 🎯 里程碑：做完即"手写出一个最小 AI Agent"

**阶段一结束你会拥有**：能调大模型 / 会写 Prompt 拿结构化 JSON / 会 Function Calling / 手写过最小 Agent。

---

## 阶段二：AI 应用开发 —— RAG（约 6-8 周）

**总目标**：独立做出一个"企业知识库问答"应用（当前企业落地最多的场景）。

- [ ] **RAG 核心概念**：为什么需要 RAG（对付幻觉 + 私有知识）
- [ ] **Embedding**：文本向量化原理
- [ ] **向量数据库**：Milvus / Qdrant / pgvector（先选一个）
- [ ] **检索优化**：文档切分（chunking）、召回、重排（rerank）
- [ ] **开发框架**：LangChain 或 LlamaIndex（Python 生态先学）
- [ ] **Java 过渡方案**（可选但推荐）：Spring AI / LangChain4j —— 用 Java 复用后端能力
- [ ] **产出**：企业文档问答机器人，跑通"上传文档 → 向量化 → 提问 → 基于文档回答 + 给出出处"完整链路

---

## 阶段三：AI Agent 开发（约 6-8 周）—— 核心竞争力

**总目标**：理解并能开发智能体。

- [ ] **Agent 核心概念**：ReAct、Function Calling、Planning、Memory
- [ ] **多智能体**：Multi-Agent 协作、任务编排
- [ ] **主流框架**：LangGraph / AutoGen / CrewAI
- [ ] **MCP（Model Context Protocol）**：了解并动手接一个
- [ ] **工程化**：Agent 可观测性、评估（Eval）、成本控制、幻觉治理
- [ ] **产出**：一个能自主规划 + 调用多个工具完成任务的 Agent（如"自动查数据 + 生成报告"）
- [ ] 观察学习：多观察 Claude Code / Codex 如何规划任务、调用工具

---

## 阶段四：够用的底层原理（全程穿插，不必等）

**目标**：理解到"能和人对话、能排查问题"的程度，不手推数学。

- [ ] 神经网络 / 反向传播（概念级）
- [ ] ⭐ Transformer 架构（重点理解 Attention 的直觉）
- [ ] 预训练 / SFT / RLHF 是怎么回事
- [ ] 分词（Tokenizer）、上下文窗口、为什么会有幻觉
- [ ] 微调入门：LoRA / QLoRA（了解即可，企业多用 RAG 而非微调）
- [ ] 推荐资源：李宏毅《机器学习》、3Blue1Brown 神经网络系列视频

---

## 阶段五：工程化与求职（持续）

**目标**：把后端优势最大化，产出可求职的项目。

- [ ] 模型部署：vLLM / Ollama / 量化
- [ ] AI 网关 / API 服务化（复用 Spring Boot 能力）
- [ ] LLMOps：Prompt 版本管理、监控、A/B 测试
- [ ] **产出 2-3 个能写进简历的完整项目**（GitHub 开源 + 部署上线）
- [ ] 简历包装：突出"Java 后端 + AI 应用"稀缺组合
- [ ] 开始投递

---

## 关键认知（贯穿始终）

1. **幻觉治理** = 少让模型"凭记忆答"，多让它"基于事实答 + 结果可验证"（RAG / 引用来源 / Tool Use / 人工校验）。
2. **Skill / Agent** = 把专业流程 + 上下文封装给 AI，让它更专业、更一致、更少幻觉。
3. **上下文管理** = 只喂给模型当前任务需要的信息（本文件的"分窗口学习"就是实践）。
4. **AI 是加速器，业务判断的"最后一公里"靠人** —— 你的工程 + 业务理解才是护城河。

---

## 跨窗口协作约定（给新窗口的 Claude）

- 新窗口开始时，我会说"读一下这个文件，我要做 Week X / 阶段 X"。
- 请你：① 读完本文件了解全局 → ② 聚焦当前任务，带我动手做 → ③ 完成后把对应 `[ ]` 改成 `[x]` → ④ 在下方"进度日志"追加一行记录。
- **每个阶段/Week 维护一份 `学习记录.md`（操作流水账）**：放在对应周的目录下（如 `week1/学习记录.md`）。**每带我动手完成一步，就往里追加一节**，记录"做了什么 / 用的命令 / 关键概念（对照 Java）/ 遇到的坑"，方便日后回看复现。
- 原则：**动手优先，每个任务都要有可运行的产出物。**

---

## 进度日志

| 日期       | 完成内容                                                                                                                                                                                                                                                                                                                                   | 备注 / 产出物位置                                         |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| 2026-07-09 | 制定完整学习路线，建立本文件                                                                                                                                                                                                                                                                                                               | 本文件                                                    |
| 2026-07-10 | Week 1 完成（一步步手把手重做）：git 全流程(init→配身份→.gitignore→commit→push GitHub)、装 PyCharm、list/dict/set、列表推导式+f-string、函数+*args/**kwargs+装饰器(≈AOP)、类型注解、requests 调 GitHub API                                                                                                                            | `week1/01~06_*.py`、`week1/学习记录.md`（详细流水账） |
| 2026-07-12 | Week 2 完成（一步步手把手）：申请 DeepSeek Key、.env+.gitignore 安全存放密钥、python-dotenv 读取、requests 裸调大模型看清 HTTP 本质、换 openai SDK(兼容生态)、理解 token/上下文窗口/计费、多轮对话记忆(手动维护 messages)、流式输出(stream/delta/flush)、temperature 实验(附"temp=0 不保证完全可复现"的工程认知)。产出命令行多轮对话小程序 | `week2/01~06_*.py`、`week2/学习记录.md`（详细流水账） |
|            |                                                                                                                                                                                                                                                                                                                                            |                                                           |
